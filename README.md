# Ankora

Clean copy of the Relay snapshot app (Ankora branding). Serves `ankora.html`
verbatim at `/`; the original `/amplify/_next` chunks in `public/` hydrate the
page so every animation runs identically to the source.

    npm install   # if node_modules not present
    npm run dev   # http://localhost:3200
